1. taking string input after Integer/float/double input
   -> before that take an empty line input.

   ```java

       int a = sc.nextInt();
       sc.nextLine(); // here
       String str = sc.nextLine();
       int b = sc.nextInt();

   ```
